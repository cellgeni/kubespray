apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: placental-organoid-experiment-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5G
  storageClassName: "glusterfs"