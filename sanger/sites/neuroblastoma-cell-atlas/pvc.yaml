apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neuroblastoma-cell-atlas-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2G
  storageClassName: "glusterfs"