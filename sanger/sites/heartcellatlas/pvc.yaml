apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heart-cell-atlas-cellxgene-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: "glusterfs"