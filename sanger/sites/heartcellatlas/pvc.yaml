apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heart-cell-atlas-cellxgene-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: "glusterfs"