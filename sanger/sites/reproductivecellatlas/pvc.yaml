apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reproductivecellatlas-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
  storageClassName: "glusterfs"